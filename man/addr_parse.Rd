% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{addr_parse}
\alias{addr_parse}
\title{Read an address literal under every dialect at once}
\usage{
addr_parse(x)
}
\arguments{
\item{x}{A character vector of address literals.}
}
\value{
A \code{raddr_parse} vector with one element per input, carrying \code{input},
the four per-dialect readings, the per-dialect \code{outcome} and \code{codes}, and
the derived \code{status}.
}
\description{
\code{addr_parse()} is raddr's primary answer. It reads each literal under all
four dialect primitives and reports every reading, the outcome of each, and
the reason codes behind each rejection -- rather than picking one reading and
discarding the rest.
}
\section{Why the outcome is per-dialect}{


Because a single one cannot be written down honestly. \code{"4294967296"} is
accepted by \code{aton} as \verb{0.0.0.0}, rejected by \code{whatwg} as out of range, and
rejected by \code{strict} as not a dotted quad -- simultaneously, on one machine.
A record with one status has to choose which of those three to report, and
every choice is a lie about the other two.
}

\section{The derived status}{


\code{\link[=addr_status]{addr_status()}} does collapse the four outcomes to one value, as a
\strong{convenience and never as truth}:

\describe{
\item{\code{ok}}{Every primitive with a say accepts, and they yield the same
address.}
\item{\code{divergent}}{They are not unanimous -- on the value, or on whether to
accept at all. \code{"4294967296"} is \code{divergent}.}
\item{\code{not_an_address}}{No primitive treats the input as an attempt at an
address. \code{"example.com"} is \code{not_an_address}.}
\item{\code{malformed}}{At least one primitive treats it as an attempt, and none
accepts.}
}

"With a say" is doing work in the first of those. \code{inet_aton} is \code{AF_INET} by
signature, so it has no reading of \code{"::1"} to withhold and its silence there
is not dissent -- otherwise every IPv6 address on earth would be \code{divergent}.
A dialect that \emph{has} the grammar and still declines is a different matter:
\code{"1.2.3.4 junk"} is \code{divergent}, because \code{aton} finds an address in it that
the other three do not, which is the class where curl reaches a host a
browser will not dial.

Whenever the answer matters, read the per-dialect outcome instead.
}

\section{Printing}{


The print method is quiet when the dialects agree and loud when they do not:
a vector of ordinary addresses prints as a column of addresses, and a
divergent row is expanded underneath to show what each dialect made of it.
There is no mode to select and nothing to force -- see \link{dialects} for why the
dialect is a function name rather than an argument.
}

\examples{
# One string, four readings
addr_parse("0177.0.0.1")

# Accepted by one dialect, rejected by three, for two different reasons
p <- addr_parse("4294967296")
addr_status(p)
addr_codes(p)

# Agreement prints quietly
addr_parse(c("127.0.0.1", "::1"))

}
\seealso{
\code{\link[=addr_reading]{addr_reading()}} to pull one dialect's reading back out,
\code{\link[=addr_codes_registry]{addr_codes_registry()}} for the reason-code vocabulary, and \link{dialects} for
the single-dialect shortcuts.
}
