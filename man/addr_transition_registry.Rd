% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition.R
\name{addr_transition_registry}
\alias{addr_transition_registry}
\alias{addr_transition_version}
\title{The transition-prefix overlay}
\usage{
addr_transition_registry(what = c("prefixes", "embeddings"))

addr_transition_version()
}
\arguments{
\item{what}{Which table to return: \code{"prefixes"} (default) or \code{"embeddings"}.}
}
\value{
A data frame. For \code{"prefixes"}: \code{block}, \code{kind}, \code{rfc}, \code{note}. For
\code{"embeddings"}: \code{kind}, \code{role}, \code{prefix_len}, \code{offset}, \code{length},
\code{complement}, where \code{offset} and \code{length} are bit positions counted from
the most significant bit of the 128-bit address.
}
\description{
The prefixes whose classification needs more granularity than the IANA
special-purpose registries provide, and the bit geometry of the IPv4
addresses embedded in them.
}
\section{Why an overlay exists at all}{


IANA records \verb{Globally Reachable} as \code{N/A} for Teredo (\verb{2001::/32}) and 6to4
(\verb{2002::/16}) -- see \code{\link[=addr_registry]{addr_registry()}}. That is not an omission: reachability
follows the \strong{embedded} IPv4 address, which no prefix table can express.
IANA is marking the point where table lookup stops being sufficient, and this
overlay is what raddr uses past that point.
}

\section{Separately stamped}{


This table has its own version, independent of
\code{\link[=addr_registry_version]{addr_registry_version()}}. The two change for unrelated reasons -- one when
IANA republishes, the other when a maintainer transcribes another RFC -- so
neither stamp is evidence about the other. There is no
\code{addr_transition_outdated()}: the RFCs this is drawn from do not expire.
}

\section{Prefixes and embeddings are different shapes}{


\code{what = "prefixes"} gives fixed prefixes with a \code{kind}. \code{what = "embeddings"}
gives one row per \strong{contiguous segment} of an embedded IPv4 address, which
is not always one row per form:

\describe{
\item{Teredo carries two addresses}{a server, in the clear, and a client
stored bitwise-complemented so a NAT will not rewrite it
(\code{complement = TRUE}).}
\item{NAT64 geometry follows the prefix length, not a prefix}{RFC 6052
permits six lengths, and a network-specific prefix may be any prefix of
one of them -- so those rows carry a \code{prefix_len} and no block. At /40,
/48 and /56 the embedded address \strong{straddles the reserved u-byte} at
bits 64-71 and arrives in two segments, most significant first.}
\item{ISATAP has an embedding but no prefix}{it is an interface-identifier
pattern (\verb{0000:5efe} or \verb{0200:5efe}) that can sit under any \verb{/64}.}
}
}

\examples{
addr_transition_registry()

# The two forms IANA declines to answer for
reg <- addr_registry()
reg[is.na(reg$globally_reachable) & is.na(reg$termination_date), "block"]

# RFC 6052's split geometry: two segments at /40, /48 and /56
emb <- addr_transition_registry("embeddings")
emb[emb$kind == "nat64", c("prefix_len", "offset", "length")]

}
\seealso{
\code{\link[=addr_registry]{addr_registry()}} for the IANA table this overlays.
}
