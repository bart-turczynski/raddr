% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{addr_registry_snapshot}
\alias{addr_registry_snapshot}
\title{Content-addressed identity of the bundled registry snapshot}
\usage{
addr_registry_snapshot()
}
\value{
A length-1 \code{character} of the form \code{"sha256:"} followed by 64 hex
digits, or \code{NA_character_} if the installed snapshot records no id.
}
\description{
Returns one string identifying exactly which vendored IANA bytes are
installed: a \code{"sha256:..."} digest over all four registry files. This is the
value to quote in a bug report, because it pins the data a result came from
without depending on the package version.
}
\section{What it answers, and what it does not}{


It answers \strong{which bytes}. Two installations reporting the same id have the
same four files, byte for byte.

It does \strong{not} answer which of two snapshots is newer. A hash has no order.
Currency is what \code{\link[=addr_registry_version]{addr_registry_version()}} and
\code{\link[=addr_address_space_version]{addr_address_space_version()}} report, and those two are deliberately
separate because they make claims about separate tables.

One id covers all four files for that same reason inverted. A \emph{date} spanning
both pairs would make each half assert currency for a table it says nothing
about; a content hash asserts only what is installed, which is a property of
the payload as a whole.
}

\section{How it is computed}{


A sha256 over a canonical manifest: one \code{"<key> sha256:<hex>"} line per
source, each terminated by a newline, in the fixed order \code{v4}, \code{v6},
\code{v4_space}, \code{v6_space}, hashed as UTF-8 bytes.

The order and the spelling are part of the definition rather than formatting,
which is what makes the id reproducible outside R -- the manifest is a byte
string anything can build from the installed CSVs and hash. The manifest is
also stored beside the id, and \code{data-raw/build-registry.R --check} verifies
both steps: that the manifest still describes the files on disk, and that the
id still follows from the manifest. Being content rather than dates, both
belong in that guard, which never compares dates.
}

\examples{
addr_registry_snapshot()

}
\seealso{
\code{\link[=addr_registry_version]{addr_registry_version()}} and \code{\link[=addr_address_space_version]{addr_address_space_version()}} for
currency rather than identity.
}
