% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{parse-accessors}
\alias{parse-accessors}
\alias{addr_reading}
\alias{addr_outcome}
\alias{addr_codes}
\alias{addr_status}
\alias{addr_is_divergent}
\alias{addr_input}
\title{Pull one dialect's reading, outcome or reason codes back out}
\usage{
addr_reading(x, dialect)

addr_outcome(x, dialect)

addr_codes(x, dialect = NULL)

addr_status(x)

addr_is_divergent(x)

addr_input(x)
}
\arguments{
\item{x}{A \code{raddr_parse} vector.}

\item{dialect}{One of \code{"strict"}, \code{"whatwg"}, \code{"pton"}, \code{"aton"},
\code{"getaddrinfo"} or \code{"curl"}. For \code{addr_codes()}, \code{NULL} unions every
dialect's codes.}
}
\value{
\code{addr_reading()} a \code{raddr_address}; \code{addr_outcome()} a factor with levels
\code{"ok"}, \code{"rejected"} and \code{"not_an_address"}; \code{addr_codes()} a list of
character vectors; \code{addr_status()} a factor with levels \code{"ok"},
\code{"divergent"}, \code{"not_an_address"} and \code{"malformed"};
\code{addr_is_divergent()} a logical vector.
}
\description{
Accessors on a \code{\link[=addr_parse]{addr_parse()}} result. All of them admit the two compositions
as well as the four primitives: the record stores only the primitives, and
\code{getaddrinfo} and \code{curl} are resolved from those on request (section 3.2).
}
\details{
\code{dialect} is a \strong{view selector on output, not a leniency knob on input}. The
parsing already happened, under every dialect, and choosing one here only
chooses which of the finished readings to look at. That is why there is a
dialect argument on these and not on \code{\link[=addr_parse]{addr_parse()}}.
}
\examples{
p <- addr_parse(c("0177.0.0.1", "127.0.0.1", "example.com"))

addr_reading(p, "whatwg")
addr_reading(p, "curl")
addr_outcome(p, "strict")
addr_codes(p, "strict")
addr_status(p)
addr_is_divergent(p)

}
