% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{addr_format}
\alias{addr_format}
\alias{addr_expand}
\title{Render addresses as text}
\usage{
addr_format(x)

addr_expand(x)
}
\arguments{
\item{x}{A \code{raddr_address} vector.}
}
\value{
A character vector the same length as \code{x}, \code{NA} for missing
addresses.
}
\description{
\code{addr_format()} renders an address in its \strong{canonical} form: RFC 5952 for
IPv6, dotted-quad for IPv4. Every address has exactly one canonical spelling,
so two addresses that compare equal always format identically. This is what
\code{\link[=format]{format()}} and \code{\link[=as.character]{as.character()}} emit.
}
\details{
\code{addr_expand()} renders the \strong{fully expanded} form instead: eight
four-digit hextets, nothing compressed, nothing abbreviated. Reach for it
when addresses have to line up in a column, sort as text, or be matched by a
prefix -- none of which the canonical form supports, because it is
variable-width by design.
}
\section{What RFC 5952 asks for}{


\describe{
\item{4.1}{Leading zeros in a field are suppressed: \verb{2001:0db8} is
\code{2001:db8}, and an all-zero field is \code{0}.}
\item{4.2.1}{The \code{::} is used wherever it can be.}
\item{4.2.2}{But never for a \emph{single} zero field -- \verb{2001:db8:0:1::1}, not
\verb{2001:db8::1::1}, and \code{2001:db8:0:1:1:1:1:1} keeps its \code{0}.}
\item{4.2.3}{The longest run of zero fields is the one compressed, and the
\strong{first} of two equally long runs wins.}
\item{4.3}{Hex digits are lowercase.}
\item{5}{An address with an embedded IPv4 address is rendered in the mixed
form: \verb{::ffff:192.0.2.1}.}
}

RFC 5952 publishes no test vectors; \code{tests/testthat/test-format.R} carries
the ones raddr authored against its text, section by section.
}

\section{The 4-in-6 form}{


The mixed form is emitted for the \code{v6_4in6} family, which is decided by the
bits rather than by the spelling (see \code{\link[=raddr_address]{raddr_address()}}). That is what makes
\code{parse(format(x)) == x} hold for \verb{::ffff:192.0.2.1}: it round-trips back into
its own family rather than collapsing onto the bare IPv4 address.
}

\section{The zone}{


A zone ID is appended as \verb{\%zone} by both renderers, and neither reads it back
into the address bits. A missing address renders as \code{NA}.
}

\examples{
a <- addr_strict(c("2001:db8::1", "::ffff:192.0.2.1", "192.0.2.1"))
addr_format(a)
addr_expand(a)

# The canonical form round-trips
addr_strict(addr_format(a)) == a

}
