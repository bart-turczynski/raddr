% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integer.R
\name{addr_to_integer}
\alias{addr_to_integer}
\alias{integer_to_addr}
\title{Encode and decode addresses as unsigned integers}
\usage{
addr_to_integer(x, output = c("character", "double", "bignum"))

integer_to_addr(x, family)
}
\arguments{
\item{x}{For \code{addr_to_integer()}, a \code{raddr_address} vector. For
\code{integer_to_addr()}, a character vector of decimal digits, a numeric
vector, or anything whose \code{as.character()} is decimal digits -- a
\code{bignum::biginteger()}, for instance. Not a \code{raw} vector; see below.}

\item{output}{One of \code{"character"} (the default), \code{"double"} or \code{"bignum"}.}

\item{family}{The family the number is to be read as: \code{"v4"}, \code{"v6"} or
\code{"v6_4in6"}, length 1 or \code{length(x)}. Required.}
}
\value{
\code{addr_to_integer()} returns a character, double or \code{biginteger}
vector as \code{output} asks, \code{NA} for a missing address. \code{integer_to_addr()}
returns a \code{raddr_address} vector.
}
\description{
\code{addr_to_integer()} is the numeric value of an address: the 4 octets of an
IPv4 address read big-endian (RFC 4632 §3.1), or the 16 octets of an IPv6
address (RFC 4291 §2). \code{integer_to_addr()} reads one back.
}
\section{R has no unsigned integer, which decides the default}{


R's \code{integer} is \strong{signed 32-bit}, so it holds barely half the IPv4 space --
\code{as.integer(4294967295)} is \code{NA} -- and R's \code{double} is exact only to 2^53,
which is comfortable for IPv4 and twenty-five orders of magnitude short for
IPv6. The carrier that always works is a decimal \strong{string}, so that is what
\code{output = "character"}, the default, returns.

\describe{
\item{\code{"character"}}{Decimal digits, no padding, no separators. Always
available, exact for both families.}
\item{\code{"double"}}{Exact for IPv4 and \strong{\code{NA} for IPv6}, including the
4-in-6 family, whose value is 128 bits like any other address. A double
cannot carry an IPv6 address, so raddr returns nothing rather than
something close.}
\item{\code{"bignum"}}{A \code{bignum::biginteger()}. The only output that needs an
installed package, and the only one that can fail -- see below.}
}
}

\section{The bignum dependency is optional, and actually optional}{


\code{bignum} is in \code{Suggests}, and the two default-reachable outputs never touch
it. The comparison worth stating: \code{ipaddress::ip_to_integer()} calls
\code{check_installed("bignum")} before doing anything, so without that package it
errors -- including for IPv4, where no arbitrary-precision arithmetic is
needed at all (verified 2026-07-28, ipaddress 1.0.3). raddr does its own
arithmetic in base 10^6 over the four 32-bit words, so you can encode and
decode every address of either family with nothing installed.

\code{output = "bignum"} does require the package, and \strong{errors} when it is
missing rather than quietly handing back the character vector. The digits
would be right and the answers would not: character ordering is
lexicographic, so \code{max()} of \code{c("9", "16777216")} is \code{"9"} and \code{sort()} puts
10 before 9. A caller who asked for numbers and silently received text gets a
wrong answer out of the first thing they do with it. The error names the
install command and the \code{"character"} alternative.
}

\section{What bignum shows you is not what it stores}{


\code{bignum} displays 7 significant figures by default, and its \code{as.character()}
and \code{format()} follow the display -- so a \code{biginteger} holding
\code{42540766411282592856903984951653826561} prints, formats, coerces and
\code{write.csv()}s as \code{"4.254077e+37"}. The stored value is exact and arithmetic
on it is exact; only the rendering rounds. Use
\code{format(x, notation = "dec")}, or raise \code{options(bignum.sigfig)}, to see all
of it. \code{integer_to_addr()} reads a \code{biginteger} by its decimal notation for
this reason, so the round trip is unaffected.
}

\section{The family does not travel in the number, so you must pass it}{


\code{integer_to_addr()} requires \code{family}, and has no default. One integer names
three different objects: \code{3221225985} is \verb{192.0.2.1} as IPv4, and as a
128-bit value the deprecated IPv4-compatible \verb{::192.0.2.1} (RFC 4291 §2.5.5),
while \verb{::ffff:192.0.2.1} is a fourth thing again. Nothing in the digits says
which, so raddr does not guess -- \code{ipaddress::integer_to_ip()} takes
\code{is_ipv6 = NULL} and infers one.

\code{family} accepts \code{"v4"}, \code{"v6"} and \code{"v6_4in6"}, scalar or one per element,
and takes the factor from \code{\link[=addr_family]{addr_family()}} directly. \code{"v6"} and \code{"v6_4in6"}
both mean 128 bits: which of the two families comes back is decided by the
bits, exactly as it is when parsing a literal.
}

\section{Out of range is \code{NA}, and so is anything that is not a number}{


A value of 2^32 or more with \code{family = "v4"}, 2^128 or more with an IPv6
family, a negative number, a sign, an exponent, a decimal point, or empty
text all decode to \code{NA}. So does a \code{double} above 2^53, because such a double
has already lost the value it was meant to carry -- raddr will not decode the
nearest representable number instead. Leading zeros and surrounding
whitespace are accepted, being unambiguous in a decimal integer.

Like the other decoders in \code{\link[=addr_to_bytes]{addr_to_bytes()}}, \code{integer_to_addr()} signals
nothing about a \emph{value} it cannot read: the answer is a missing address. A
wrong \emph{type} is a different matter and errors, as it does everywhere else in
raddr. A \code{raw} vector is the case worth naming, because its \code{as.character()}
is hexadecimal -- reading \code{as.raw(16)} as a number would silently yield
\verb{0.0.0.10}. Bytes go to \code{\link[=bytes_to_addr]{bytes_to_addr()}}, which knows they are bytes.
}

\examples{
a <- addr_pton(c("192.0.2.1", "2001:db8::1", "::ffff:192.0.2.1"))
addr_to_integer(a)

# Exact in a double for IPv4, and NA rather than lossy for IPv6
addr_to_integer(a, output = "double")

# The family has to be carried alongside the number
integer_to_addr(3221225985, family = "v4")
integer_to_addr(3221225985, family = "v6")

# Which makes the round trip this
integer_to_addr(addr_to_integer(a), addr_family(a)) == a

# The largest address of each family
addr_to_integer(addr_pton("ffff:ffff:ffff:ffff:ffff:ffff:ffff:ffff"))

}
\seealso{
\code{\link[=addr_to_bytes]{addr_to_bytes()}} for the byte, hex and binary pairs.
}
