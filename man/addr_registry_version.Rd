% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{addr_registry_version}
\alias{addr_registry_version}
\alias{addr_registry_outdated}
\title{Provenance of the bundled registry snapshot}
\usage{
addr_registry_version()

addr_registry_outdated(max_age = 365)
}
\arguments{
\item{max_age}{Maximum acceptable age in days. Default 365.}
}
\value{
\code{addr_registry_version()} returns a length-1 \code{character}
\code{"YYYY-MM-DD"} date, or \code{NA_character_} when the snapshot is undated.
\code{addr_registry_outdated()} returns a length-1 \code{logical}, \code{TRUE} when the
snapshot is older than \code{max_age} days \strong{or} undated.
}
\description{
\code{addr_registry_version()} reports when the vendored registries were last
changed upstream. \code{addr_registry_outdated()} says whether that is longer ago
than \code{max_age} days.
}
\section{Unknown is not fresh}{


The IANA CSVs carry no version field, so the stamp is the \code{Last-Modified}
date the two files were served with, normalized at build time. When either
half has no date, the snapshot has no date: \code{addr_registry_version()} returns
\code{NA} and \code{addr_registry_outdated()} returns \code{TRUE}.

That asymmetry is deliberate. A snapshot of unknown age is one you have no
evidence about, and treating no evidence as evidence of freshness is the one
failure mode a staleness check exists to prevent.

The stamp is also the \strong{older} of the two halves, because a snapshot is only
as current as its stalest part.
}

\section{No refresh}{


There is no \code{addr_registry_refresh()}. raddr performs no network access at
all: the registries change on a multi-year cadence and the whole vendored
payload is 4.7 KB, so shipping it outright is a cleaner claim than network
code that defaults to off. A stale snapshot is fixed by upgrading the
package.
}

\examples{
addr_registry_version()
addr_registry_outdated()

# An undated or overly old snapshot is reported, never assumed fresh
addr_registry_outdated(max_age = 0)

}
\seealso{
\code{\link[=addr_registry]{addr_registry()}} for the data itself.
}
