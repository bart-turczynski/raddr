% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{addr_classify}
\alias{addr_classify}
\title{Classify addresses against the IANA registries}
\usage{
addr_classify(x)
}
\arguments{
\item{x}{A \code{raddr_address} vector.}
}
\value{
A \code{raddr_class} vector, one element per address. Every field is \code{NA}
for a missing address.
}
\description{
Reports what the IANA registries say about each address: the block that
matched, its name and RFC, raddr's own one-word \code{category}, all five IANA
policy columns, the transition mechanism the address belongs to, and the
snapshot the answer came from.
}
\details{
\code{addr_classify()} returns facts. It returns no verdict, no risk score and no
allow-or-deny decision, and it ships no "everything that is not \code{global}"
helper. Policy belongs to the consumer (P8).
}
\section{Two registries, and which one answered}{


Classification is \strong{total}: every non-missing address matches some row.
That takes two layers, because the IANA special-purpose registries do not
cover the whole address space -- \verb{224.0.0.0/4} and \verb{ff00::/8} appear in
neither, which is how \code{ssrfcheck} shipped CVE-2025-8267.

\describe{
\item{\code{\link[=addr_registry]{addr_registry()}}, the special-purpose pair}{Answers \strong{policy}: the
five logical columns. Matched first, and it outranks the other layer on
IANA's own instruction -- the address-space registries carry "For
authoritative registration, see [Special-Purpose Address Space]".}
\item{\code{\link[=addr_address_space]{addr_address_space()}}, the address-space pair}{Answers
\strong{identity}: what a range is for and who holds it. Each half is an exact
partition, which is what makes the lookup total.}
}

\code{registry} says which one answered, and \code{registry_version} carries that
layer's own stamp (P7). The distinction is load-bearing, because it is what
keeps the two meanings of \code{NA} apart in the five policy columns:

\describe{
\item{\code{registry = "special_purpose"}}{\code{NA} is IANA's own \code{N/A} -- a policy
it specifically declined to state. Reading it as \code{FALSE} asserts
something the registry withheld.}
\item{\code{registry = "address_space"}}{\code{NA} means the question was never
asked: that registry has no policy columns at all. raddr leaves them
absent rather than inventing them.}
}
}

\section{Every \code{NA} says why it is \code{NA}}{


Where raddr knows that two missing values mean different things, it reports
what distinguishes them rather than leaving both blank. Four special-purpose
blocks have a missing policy value, for three different reasons, and each
reason is a column:

\describe{
\item{Deprecated: \verb{192.88.99.0/24}, \verb{2001:10::/28}}{all five columns are
\code{NA} and \code{termination_date} is set. IANA gives a withdrawn block no
policy at all.}
\item{Withheld: \verb{2001::/32} (Teredo), footnote \verb{[2]}}{RFC 4380 section 5
makes relay advertisement voluntary and per-deployment, so no bits in the
address answer reachability.}
\item{Withheld: \verb{2002::/16} (6to4), footnote \verb{[3]}}{a different reason
entirely -- reachability follows the \emph{embedded} IPv4 address, which a
prefix table cannot express.}
}

\code{footnotes} does the same job for \code{rfc}, which is \code{NA} for every IPv4
address-space row. 42 of those 256 rows carry a footnote marker, meaning the
citation exists and its text is on the registry page rather than in the CSV;
the other 214 carry none. raddr reports that a caveat exists rather than
inventing its wording, and \code{""} means the row carried no marker at all.

Footnote numbering is \strong{per registry}, so a marker is only meaningful
alongside \code{registry} and the address family.
}

\section{What the fields are}{


\describe{
\item{\code{block}, \code{name}, \code{rfc}, \code{footnotes}}{The matched row, as vendored.
\code{rfc} is \code{NA} for every IPv4 address-space row, because that registry has
no reference column; \code{footnotes} is what says whether a citation
nonetheless exists.}
\item{\code{category}}{raddr's own one-word vocabulary, 19 levels. It is
\strong{descriptive, not a policy input} -- see \code{\link[=addr_category_map]{addr_category_map()}}, and do
not build a deny-list of level names on it.}
\item{\code{globally_reachable}, \code{forwardable}, \code{source}, \code{destination},
\code{reserved_by_protocol}}{IANA's five policy columns, per row and never
collapsed. \code{globally_reachable} \strong{is} the column, not a derived
\code{is_global}.}
\item{\code{termination_date}}{Set on a deprecated block, and the reason its
policy columns are empty. \code{NA} everywhere else, including for every
address-space row.}
\item{\code{embedded_kind}}{The transition mechanism, when a mechanism prefix
matched. \code{NA} means no prefix matched -- it never means "not NAT64". A
caller-supplied RFC 6052 network-specific prefix is invisible to a prefix
table, so raddr states a NAT64 kind only affirmatively.}
\item{\code{embeddings}}{Zero or more extracted inner addresses, one
\code{raddr_embedding} per element, each carrying the extracted address and
\strong{its own} \code{category}. Plural because a Teredo address carries \strong{two}
IPv4 addresses -- a server in the clear and a bitwise-complemented
client -- and raddr does not choose between them.}
\item{\code{codes}}{Classify-layer reason codes, from the same vocabulary as
\code{\link[=addr_codes_registry]{addr_codes_registry()}} and graded by that registry's \code{strength}
column. See below.}
}
}

\section{The codes are graded, and all of them are reported}{


\code{codes} carries what the RFCs say about an address that the registry row
alone does not. Each one is graded in \code{\link[=addr_codes_registry]{addr_codes_registry()}} by the force
of the rule it reports, because reporting only the MUST rules would collapse
a spectrum into a binary:

\describe{
\item{\code{link_local_outside_fe80_64} (\code{must})}{\verb{febf::1} matches the
\verb{fe80::/10} registry row but is not a link-local address -- RFC 4291
section 2.5.6 fixes the next 54 bits to zero. Both CPython's
\code{ipaddress} and R's \code{ipaddress} report it as link-local with nothing
attached to say otherwise.}
\item{\code{link_local_reserved_range} (\code{must})}{\verb{169.254.0.0/24} and
\verb{169.254.255.0/24} MUST NOT be selected by IPv4 autoconfiguration
(RFC 3927 section 2.1). Neither has a registry row of its own.}
\item{\code{ipv4_compatible_low_tail} (\code{may})}{the deprecated \verb{::a.b.c.d} tail
lands in \verb{0.0.0.0/8}, so it is not a host address.}
\item{\code{nat64_local_layout_unspecified} (\code{unspecified})}{RFC 8215 section 5
leaves the syntax under \verb{64:ff9b:1::/48} deliberately undefined, so the
RFC 6052 geometry raddr reads there is contested rather than implied.}
\item{\code{ula_l_bit_unset} (\code{unspecified})}{\verb{fc00::/8} is the L = 0 half of
the ULA prefix, for which RFC 4193 section 3.1 defines nothing at all.
Only \verb{fd00::/8} is a specified ULA.}
}

Three further classify codes are registered and are not emitted yet:
\code{nat64_wk_embedded_not_global}, \code{sixtofour_embedded_not_global} and
\code{teredo_client_not_global} each need the embedded IPv4 extracted and
classified. They are registered now so that filling \code{embeddings} later adds
no code to the vocabulary.
}

\section{A string may never be classified}{


\code{addr_classify()} takes a parsed address and nothing else (P1). It also
declines a \code{raddr_parse}, which holds four readings that may be four
different addresses: choosing one is a decision, and raddr makes it by
function name rather than silently. Pick a reading with
\code{\link[=addr_reading]{addr_reading()}}, or parse with \code{\link[=addr_strict]{addr_strict()}}, \code{\link[=addr_whatwg]{addr_whatwg()}},
\code{\link[=addr_pton]{addr_pton()}} or \code{\link[=addr_aton]{addr_aton()}}.
}

\examples{
addr_classify(addr_pton(c("127.0.0.1", "8.8.8.8", "224.0.0.1", "4000::1")))

# All four facts about the NAT64 well-known prefix, none collapsed
cl <- addr_classify(addr_pton("64:ff9b::a9fe:a9fe"))
as.data.frame(cl)[c("block", "category", "globally_reachable")]

# The carve-out that forces longest-prefix matching
as.data.frame(addr_classify(addr_pton(c("192.0.0.9", "192.0.0.100"))))

}
\seealso{
\code{\link[=addr_category]{addr_category()}} and \code{\link[=addr_embeddings]{addr_embeddings()}} for single fields,
\code{\link[=addr_registry]{addr_registry()}} and \code{\link[=addr_address_space]{addr_address_space()}} for the data behind the
answer.
}
