% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{addr_global_reachability}
\alias{addr_global_reachability}
\title{Whether an address is in globally reachable space}
\usage{
addr_global_reachability(x)
}
\arguments{
\item{x}{A \code{raddr_address}, \code{raddr_class} or \code{raddr_embedding} vector. An
embedding is classified by its extracted address, so the answer is the
same one the classify layer used when grading that embedding.}
}
\value{
A logical vector the same length as \code{x}: \code{TRUE}, \code{FALSE} or \code{NA}.
}
\description{
The two-layer positive fact raddr already uses internally to decide the
antecedent of RFC 6052 section 3.1, RFC 3056 section 9 and RFC 4380
section 4. It is a \strong{fact, not a verdict}: it reports what the two vendored
registry layers say, and says nothing about whether a caller should permit
the address.
}
\section{The two layers, and why both}{


Neither layer answers alone, and each fixes what the other gets wrong:

\describe{
\item{special-purpose}{IANA's own \code{globally_reachable} column, unmodified.
Without it the five blocks IANA marks globally reachable -- PCP and TURN
anycast, AS112 twice, AMT -- read as non-global.}
\item{address space}{that layer has no policy column at all, and the
question it does answer is whether the space is delegated to an RIR,
which is \code{category = "global"}. Without it \verb{8.8.8.8} reads as non-global
and \verb{224.0.0.0/4} reads as nothing -- CVE-2025-8267's shape.}
}

This is \strong{not} the \code{category} deny-list \code{\link[=addr_category]{addr_category()}} warns against. It
reads one \emph{positive} level, only in the layer that has no other column, and
a level added later changes no answer that layer gives today. Reaching this
fact through this function rather than rebuilding it from \code{category} is the
whole reason it is exported.
}

\section{\code{NA} is a third answer, not a missing one}{


\code{NA} means the registries leave the question open, and it must not be read
as \code{FALSE}. Four special-purpose blocks are in that tier (see
\code{\link[=addr_registry]{addr_registry()}}): the withdrawn \verb{192.88.99.0/24} and \verb{2001:10::/28}, which
IANA gave no policy at all, and the live 6to4 (\verb{2002::/16}) and Teredo
(\verb{2001::/32}) blocks, which IANA itself records as \code{N/A}. So every 6to4 and
every Teredo address answers \code{NA} here, whatever it wraps. Asserting a
MUST-drop there would be reading IANA's \code{N/A} as a \code{FALSE} one level down.

For 6to4 the question IANA declines is answered by the embedded IPv4 address:
pass the embedding row, \code{addr_global_reachability(addr_embeddings(x)[[1]])}.
The Teredo \code{N/A} has an unrelated cause -- relay advertisement is
per-deployment -- and no bits in the address answer it.

Handle it explicitly. R propagates \code{NA} rather than resolving it: \code{any()}
returns \code{NA} instead of \code{FALSE}, \code{which()} drops the element entirely, and
\code{if} raises an error on it. A policy layer must branch on all three values
rather than let the third fall through to either side. raddr reports it;
deciding what it costs is the caller's.
}

\examples{
# The address-space layer answers for an ordinary host, the special-purpose
# layer for a carve-out, and neither for the withdrawn anycast prefix.
addr_global_reachability(addr_pton(c("8.8.8.8", "192.0.0.9", "192.88.99.1")))

# Both families, one rule
addr_global_reachability(addr_pton(c("2001:4860:4860::8888", "fe80::1")))

# The outer address and what it embeds are separate questions: this block is
# globally reachable and the IPv4 inside it is not
a <- addr_pton("64:ff9b::a9fe:a9fe")
addr_global_reachability(a)
addr_global_reachability(addr_embeddings(a)[[1]])

}
\seealso{
\code{\link[=addr_classify]{addr_classify()}} for the whole record, including the
\code{globally_reachable} column this reads. \code{\link[=addr_category]{addr_category()}} for why the
descriptive vocabulary is not a policy input.
}
