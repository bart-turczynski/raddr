% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{addr_registry}
\alias{addr_registry}
\title{The bundled IANA special-purpose address registries}
\usage{
addr_registry()
}
\value{
A data frame of 51 rows. See the sections above for the columns.
}
\description{
Returns the two IANA special-purpose address registries as one data frame,
exactly as vendored. This is the table \code{\link[=addr_classify]{addr_classify()}} matches against.
}
\section{All five policy columns, never collapsed}{


IANA records five independent properties per block, and raddr surfaces all
five rather than reducing them to a single "is it private" flag.
\code{globally_reachable} \strong{is} IANA's \verb{Globally Reachable} column, per row, with
an RFC citation -- not a value raddr derives.

Each is \code{logical}, and \code{NA} is a real answer with a real meaning: IANA
declined to give one. Two cases produce it.

\describe{
\item{The block is deprecated}{\verb{192.88.99.0/24} and \verb{2001:10::/28} carry a
\code{termination_date} and no policy values at all.}
\item{The answer depends on something the table cannot express}{Teredo
(\verb{2001::/32}) and 6to4 (\verb{2002::/16}) are recorded as \code{N/A} for
\code{globally_reachable}, because reachability follows the \emph{embedded} IPv4
address. That is what \code{addr_embedded_scope()} is for.}
}

Reading either as \code{FALSE} would assert a policy IANA specifically withheld,
so raddr keeps them \code{NA}.
}

\section{Blocks, not rows}{


One row here is one prefix. That is not always one row upstream: the CSV
names two prefixes in a single record (\verb{192.0.0.170/32, 192.0.0.171/32}),
and three records wrap across lines because they cite more than one RFC.
The vendored 25 + 25 records become \strong{51 blocks}.
}

\section{Longest prefix, not first match}{


The registry contains deliberate carve-outs -- \verb{192.0.0.9/32} and
\verb{192.0.0.10/32} are globally reachable inside a \verb{192.0.0.0/24} that is not.
Any lookup over this table must be longest-prefix-match.
}

\section{Footnotes}{


\code{footnotes} records which upstream footnote markers a row carried, as a
space-separated string, and is \code{""} when it carried none. The footnote
\strong{text} is not in the CSV -- it lives on the registry page -- so raddr
reports that a caveat exists rather than inventing its wording.
}

\examples{
reg <- addr_registry()
nrow(reg)

# The carve-out that forces longest-prefix matching
reg[startsWith(reg$block, "192.0.0."), c("block", "globally_reachable")]

# The blocks whose reachability IANA declined to state
reg[is.na(reg$globally_reachable), c("block", "name", "termination_date")]

}
\seealso{
\code{\link[=addr_registry_version]{addr_registry_version()}} for the snapshot's provenance.
}
