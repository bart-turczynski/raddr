% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dialects.R
\name{dialects}
\alias{dialects}
\alias{addr_strict}
\alias{addr_whatwg}
\alias{addr_pton}
\alias{addr_aton}
\alias{addr_getaddrinfo}
\alias{addr_curl}
\title{Read an address literal under one dialect}
\usage{
addr_strict(x)

addr_whatwg(x)

addr_pton(x)

addr_aton(x)

addr_getaddrinfo(x)

addr_curl(x)
}
\arguments{
\item{x}{A character vector of address literals.}
}
\value{
A \code{raddr_address} vector, \code{NA} where the dialect rejects the input.
}
\description{
Six functions, one per dialect. They exist because standards and
implementations disagree about what an IP address literal means, and raddr's
answer is to show you all of the readings rather than pick one.
}
\details{
The dialect is chosen by calling a named function. There is deliberately no
\code{strict = FALSE} argument and no dialect knob buried in \code{...}: a named
function is harder to helpfully default away than an argument is.
}
\section{On paper}{


\describe{
\item{\code{addr_strict()}}{The RFC dotted-quad grammar: exactly four decimal
octets, no leading zeros, no hex, no octal, no short form. This is what
Python's \code{ipaddress}, Go and Rust accept. It is \strong{not} what
\code{inet_pton()} accepts, although the two are often conflated.}
\item{\code{addr_whatwg()}}{The WHATWG URL host parser -- what browsers do. Hex
and octal parts, one to four parts with the last filling the remainder,
and one trailing dot dropped, so \verb{1.2.3.} is \verb{1.2.0.3}. Values above
2^32 - 1 are rejected rather than wrapped.}
}
}

\section{In reality}{


\describe{
\item{\code{addr_pton()}}{POSIX \code{inet_pton()}. Four decimal parts, leading zeros
allowed and ignored, so \verb{0177.0.0.1} is \strong{177.0.0.1} and not
\verb{127.0.0.1}. This is the one dialect whose behavior varies by platform;
raddr models Apple libc, and glibc and musl are unverified.}
\item{\code{addr_aton()}}{BSD \code{inet_aton()}. Hex, octal and short forms, and
three quirks worth knowing: a whole-host number is truncated to 32 bits
rather than rejected, so \code{4294967296} is \verb{0.0.0.0}; parsing stops at the
first whitespace character and ignores the rest, so \verb{1.2.3.4 junk} is an
address; and a digitless \verb{0x} is tolerated in any part but the last.}
}
}

\section{Compositions}{


The last two are precedence orderings over the same two reality primitives,
not parsers in their own right:

\describe{
\item{\code{addr_getaddrinfo()}}{\code{pton}, falling back to \code{aton}. Whitespace is
the one place the composition leaks: \code{getaddrinfo()} rejects an input
containing whitespace outright, where bare \code{aton} would accept it.}
\item{\code{addr_curl()}}{\code{aton}, falling back to \code{pton} -- the opposite
precedence, which is the whole reason \verb{192.0.048.1} reaches a host under
curl that a browser refuses to dial.}
}
}

\section{What these do not give you}{


These are shortcuts for a caller who has already chosen a dialect. They
return a bare address, so a rejected input comes back as \code{NA} with no reason
attached. The total, outcome-bearing form -- every reading at once, with the
reason codes -- is \code{addr_parse()}, which is not written yet.

IPv6 literals are also not written yet, and are currently rejected by all
six. The package is unreleased.
}

\section{Provenance}{


The reality dialects and both compositions were measured against Apple libc
and curl 7.1.0 / libcurl 8.14.1 on macOS Darwin 25.4.0 arm64 on 2026-07-26.
\code{data-raw/oracle-ipv4.py} regenerates the measurements, and
\code{tests/testthat/test-ipv4.R} holds them as the divergence table.
}

\examples{
# One string, one machine, three different hosts
addr_strict("0177.0.0.1")
addr_whatwg("0177.0.0.1")
addr_pton("0177.0.0.1")

# curl reaches a host a browser refuses to dial
addr_whatwg("192.0.048.1")
addr_curl("192.0.048.1")

# inet_aton truncates a whole-host number instead of rejecting it
addr_aton("4294967296")

}
